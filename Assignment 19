Question 24: You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?

Hint 1:- Add the flag to show the different-2 data on click of buttons.
Hint 2:- Add text in the column for showing the name, phone number, and email.
Hint 3:- Add imageView to show the image.
Hint 4:- Add the image to the assets folder.

Source code:
import 'package:flutter/material.dart';

void main() {
  runApp(ShowDataApp());
}

class ShowDataApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: ShowDataScreen(),
    );
  }
}

class ShowDataScreen extends StatefulWidget {
  @override
  _ShowDataScreenState createState() => _ShowDataScreenState();
}

class _ShowDataScreenState extends State<ShowDataScreen> {
  bool showImage = false; // Hint 1: Flag to switch data

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Show Data Example"),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // --- Buttons A and B ---
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = true; // Show image when A is pressed
                    });
                  },
                  style: ElevatedButton.styleFrom(
                    backgroundColor: Colors.teal,
                  ),
                  child: Text("Button A"),
                ),
                SizedBox(width: 20),
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = false; // Show details when B is pressed
                    });
                  },
                  style: ElevatedButton.styleFrom(
                    backgroundColor: Colors.deepOrange,
                  ),
                  child: Text("Button B"),
                ),
              ],
            ),
            SizedBox(height: 40),

            // --- Conditional UI ---
            if (showImage)
              // Hint 3: Show image
              Image.asset(
                'assets/profile.png', // <-- your image in assets folder
                width: 200,
              )
            else
              // Hint 2: Show name, phone, email
              Column(
                children: const [
                  Text(
                    "Name: Lakshmi Narasimha",
                    style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
                  ),
                  SizedBox(height: 8),
                  Text(
                    "Phone: +91 9876543210",
                    style: TextStyle(fontSize: 18),
                  ),
                  SizedBox(height: 8),
                  Text(
                    "Email: lakshmi@example.com",
                    style: TextStyle(fontSize: 18),
                  ),
                ],
              ),
          ],
        ),
      ),
    );
  }
}

